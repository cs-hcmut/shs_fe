import { IDType } from "../_commons/id.type";

export interface HouseSchedule_DevicesForm {
  deviceAttrId: IDType;
  deviceName: string;
}

export interface HouseSchedule_ActionForm {
  value: number;
}
